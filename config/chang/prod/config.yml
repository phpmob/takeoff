chang:
    packages:
        Redis:
            monolog:
                options:
                    level: debug
                    dsn: '%env(REDIS_URL)%/0'
            session:
                options:
                    dsn: "%env(REDIS_URL)%/1"
